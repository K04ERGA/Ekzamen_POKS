//Используя команды linux, найдите все файлы в директории /etc, которые содержат слово “root” и измените их права доступа так, чтобы только владелец файла мог читать и писать в него.
find /etc -type f -name "*root*" -exec chmod 600 {} \;
